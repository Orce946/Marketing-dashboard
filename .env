# Local Development Server Configuration
# This ensures it runs on port 3000 on both Mac and Windows
VITE_PORT=3000

# Setting host to 0.0.0.0 allows access from the local network (Wi-Fi)
VITE_HOST=0.0.0.0

# Base URL for API calls (if needed in the future)
VITE_API_BASE_URL=/api
